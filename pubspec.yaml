name: office_sim
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flame: ^1.30.1
  web_socket_channel: ^3.0.3
  http: ^1.2.0
  dio: ^5.4.0
  crypto: ^3.0.3
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/office_game/furniture/desks/
    - assets/office_game/furniture/chairs/
    - assets/office_game/furniture/meeting/
    - assets/office_game/furniture/lounge/
    - assets/office_game/furniture/decor/
    - assets/office_game/ui/
    - assets/office_game/characters/programmer/walk/
    - assets/office_game/characters/programmer/idle/
    - assets/office_game/characters/programmer/sit_desk/
    - assets/office_game/characters/programmer/meeting_states/
    - assets/office_game/characters/programmer/meeting_seated/
    - assets/office_game/characters/programmer/sofa_states/
    - assets/office_game/characters/designer/walk/
    - assets/office_game/characters/designer/idle/
    - assets/office_game/characters/designer/sit_desk/
    - assets/office_game/characters/designer/meeting_states/
    - assets/office_game/characters/designer/meeting_seated/
    - assets/office_game/characters/designer/sofa_states/
    - assets/office_game/characters/pm/walk/
    - assets/office_game/characters/pm/idle/
    - assets/office_game/characters/pm/sit_desk/
    - assets/office_game/characters/pm/meeting_states/
    - assets/office_game/characters/pm/meeting_seated/
    - assets/office_game/characters/pm/sofa_states/
    - assets/office_game/characters/tester/walk/
    - assets/office_game/characters/tester/idle/
    - assets/office_game/characters/tester/sit_desk/
    - assets/office_game/characters/tester/meeting_states/
    - assets/office_game/characters/tester/meeting_seated/
    - assets/office_game/characters/tester/sofa_states/
    - assets/office_game/characters/ops/walk/
    - assets/office_game/characters/ops/idle/
    - assets/office_game/characters/ops/sit_desk/
    - assets/office_game/characters/ops/meeting_states/
    - assets/office_game/characters/ops/meeting_seated/
    - assets/office_game/characters/ops/sofa_states/
    - assets/environment/
    - assets/data/office_game_layout.json

  fonts:
    - family: LocalChinese
      fonts:
        - asset: assets/fonts/simhei.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
