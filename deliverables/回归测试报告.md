# 回归测试报告

## 测试范围

- 主视觉背景替换为参考图后是否仍可正常显示
- 工作区 / 休息区 / 讨论区功能是否保留
- 角色随机任务是否继续工作
- WebSocket 实时命令是否可接入
- MCP 指令是否能正确映射到角色状态
- HUD / 状态气泡 / 阴影 / 家具渲染是否正常

## 已执行检查

### 静态诊断

- [office_game.dart](file:///e:/testFlame/lib/game/world/office_game.dart)
- [remote_controller.dart](file:///e:/testFlame/lib/game/system/remote_controller.dart)
- [mcp_tool_adapter.dart](file:///e:/testFlame/lib/game/system/mcp_tool_adapter.dart)
- [hud.dart](file:///e:/testFlame/lib/game/ui/hud.dart)
- [pubspec.yaml](file:///e:/testFlame/pubspec.yaml)

当前已完成 IDE 诊断检查，未发现新增语法错误。

### 联网链路回归点

- `OFFICE_WS_URL` 为空时，自动进入 Mock 回退模式
- `OFFICE_WS_URL` 有值但连接失败时，自动进入降级模式
- 收到 `state_change` 消息时，应触发角色任务重分配
- 收到 `tools/call` / `mcp.tool_call` 时，应经由 MCP 适配器解析
- 周期性快照应通过 `sendSceneSnapshot()` 回传

### 视觉回归点

- 参考图主视觉不应遮挡角色
- HUD 仍显示余额、产出、状态统计
- 角色气泡与能量条继续工作
- 家具阴影与深度排序逻辑不丢失

## 待你本机最终验收的项目

- WebSocket 真实服务地址联调
- MCP 服务端真实协议兼容性
- 视觉一致性主观验收
- 网络稳定性长时间运行观察

## 结论

当前版本已具备：

- 参考图基准主视觉
- 实时联网入口
- MCP 适配层
- 原有办公场景功能保留

剩余验收重点集中在真实服务联调，而非本地工程结构缺失。
